#include <iostream>
#include <vector>
#include <string>
#include <queue>

using namespace std;

int n, m;
vector<string> v;

int dx[4] = {-1, 1, 0, 0};
int dx[4] = {0, 0, -1, 1};
pair<int, int> r

int main()
{
    cin >> n >> m;

    string temp;

    for(int i = 0; i < n; i++)
    {
        cin >> temp;

        v.push_back(temp);
    }

    
    
}